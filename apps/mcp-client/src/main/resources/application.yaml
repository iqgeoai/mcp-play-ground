spring:
  application:
    name: mcp-playground-client
    version: 1.0.0
  banner:
    location: classpath:banner.txt
  ai:
    mcp:
      client:
        enabled: true
        sse:
          connections:
            mcp-playground-server:
              url: http://localhost:8888/mcp
    ollama:
      base-url: http://localhost:11434
      chat:
        options:
          model: llama3.1

logging:
  level:
    ro:
      micronikgrupm: DEBUG
    org:
      springframework: INFO

server:
  port: 8887